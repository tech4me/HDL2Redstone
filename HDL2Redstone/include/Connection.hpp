#pragma once

namespace HDL2Redstone {
class Connection {

  public:
};

// bool addSource();
// Check connection's exsisting sinks, add port to sink
// bool addSink(Port*);
} // namespace HDL2Redstone
