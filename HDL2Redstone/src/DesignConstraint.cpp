#include <DesignConstraint.hpp>

using namespace HDL2Redstone;

bool DesignConstraint::readDCFile(const std::string& File_) {
    /*
    INIReader Reader(File_);
    if (Reader.ParserError()) {
        std::cout << "Error parsing: " << File_ << std::endl;
        return true;
    }
    */
    return false;
}
